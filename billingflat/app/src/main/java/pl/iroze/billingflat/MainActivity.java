package pl.iroze.billingflat;import androidx.appcompat.app.AppCompatActivity;import android.os.Bundle;import android.view.View;import android.widget.Button;import android.widget.TextView;import java.time.LocalDate;import java.time.Month;import pl.iroze.billingflat.model.Business;import pl.iroze.billingflat.model.Counter;import pl.iroze.billingflat.model.Example;import pl.iroze.billingflat.model.Flat;import pl.iroze.billingflat.model.RateGas;import pl.iroze.billingflat.model.State;public class MainActivity extends AppCompatActivity {    private Warehouse warehouse;    private TextView textView;    private Button buttonBack;    private FragmentStart fragmentStart = new FragmentStart();    private FragmentLogIn fragmentLogIn = new FragmentLogIn();    private FragmentFlatList fragmentFlatList = new FragmentFlatList();    private FragmentFlat fragmentFlat = new FragmentFlat();    private FragmentCounterList fragmentCounterList = new FragmentCounterList();    private FragmentCounter fragmentCounter = new FragmentCounter();    private FragmentStates fragmentStates = new FragmentStates();    private FragmentSettlement fragmentSettlement = new FragmentSettlement();    private FragmentRate fragmentRate = new FragmentRate();    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        Warehouse warehouse = new Warehouse();        warehouse.init();        setContentView(R.layout.activity_main);        textView = (TextView) findViewById(R.id.textViewMain);        buttonBack = (Button) findViewById(R.id.btn_back);        State state1 = new State(1, LocalDate.of(2019, Month.AUGUST, 1), 168.356);        State state2 = new State(1, LocalDate.of(2019, Month.SEPTEMBER, 1), 180.509);        RateGas rateGas = new RateGas(2, LocalDate.of(2018, Month.AUGUST, 28), 1.9, 8.73);        //Double x = warehouse.calclate(state1,state2, null,null);        Double x = Calculations.calculationGas(state1, state2, rateGas);        textView.setText(String.valueOf(x));        buttonBack.setOnClickListener(new View.OnClickListener() {            @Override            public void onClick(View v) {                if (mListener != null) {                    mListener.onFragemntStart();                }            }        });        fragmentStart.mListener = mListener;        fragmentLogIn.mListener = mListener;        fragmentLogIn.setWarehouse(warehouse);        fragmentFlatList.mListener = mListener;        fragmentFlatList.setWarehouse(warehouse);        fragmentFlat.mListener = mListener;        fragmentFlat.setWarehouse(warehouse);        fragmentCounterList.mListener = mListener;        fragmentCounterList.setWarehouse(warehouse);        fragmentCounter.mListener = mListener;        fragmentCounter.setWarehouse(warehouse);        fragmentStates.mListener = mListener;        fragmentStates.setWarehouse(warehouse);        fragmentSettlement.mListener = mListener;        fragmentSettlement.setWarehouse(warehouse);        fragmentRate.mListener = mListener;        fragmentRate.setWarehouse(warehouse);        getSupportFragmentManager().beginTransaction().replace(R.id.fragment_container, fragmentStart, "blank").commit();    }    OnFragmentInteractionListener mListener = new OnFragmentInteractionListener() {        @Override        public void onFragemntStart() {            //    textView.setText("FragemntStart");            getSupportFragmentManager().beginTransaction().replace(R.id.fragment_container, fragmentStart, "blank").commit();        }        @Override        public void onFragmentLogIn() {            textView.setText("FragmentLogIn");            getSupportFragmentManager().beginTransaction().replace(R.id.fragment_container, fragmentLogIn, "blank").commit();        }        @Override        public void onFragmentFlatList() {            textView.setText("FragmentFlatList");            getSupportFragmentManager().beginTransaction().replace(R.id.fragment_container, fragmentFlatList, "blank").commit();        }        @Override        public void onFragmentFlat(Flat flat) {            ;            textView.setText("FragmentFlat");            fragmentFlat.setFlat(flat);            getSupportFragmentManager().beginTransaction().replace(R.id.fragment_container, fragmentFlat, "blank").commit();        }        @Override        public void onFragmentCounterList(Flat flat) {            textView.setText("FragmentCounterList");            fragmentCounterList.setFlat(flat);            getSupportFragmentManager().beginTransaction().replace(R.id.fragment_container, fragmentCounterList, "blank").commit();        }        @Override        public void onFragmentCounter(Flat flat, Counter counter) {            textView.setText("FragmentCounter");            fragmentCounter.setFlat(flat);            fragmentCounter.setCounter(counter);            getSupportFragmentManager().beginTransaction().replace(R.id.fragment_container, fragmentCounter, "blank").commit();        }        @Override        public void onFragmentStates(Flat flat, Counter counter) {            textView.setText("FragmentStates");            fragmentStates.setFlat(flat);            fragmentStates.setCounter(counter);            getSupportFragmentManager().beginTransaction().replace(R.id.fragment_container, fragmentStates, "blank").commit();        }        @Override        public void onFragmentSettlement(Flat flat) {            textView.setText("FragmentSettlement");            fragmentSettlement.setFlat(flat);            getSupportFragmentManager().beginTransaction().replace(R.id.fragment_container, fragmentSettlement, "blank").commit();        }        @Override        public void onFragmentRate(Flat flat) {            textView.setText("FragmentRate");            fragmentRate.setFlat(flat);            getSupportFragmentManager().beginTransaction().replace(R.id.fragment_container, fragmentRate, "blank").commit();        }    };    public interface OnFragmentInteractionListener {        void onFragemntStart();        void onFragmentLogIn();        void onFragmentFlatList();        void onFragmentFlat(Flat flat);        void onFragmentCounterList(Flat flat);        void onFragmentCounter(Flat flat, Counter counter);        void onFragmentStates(Flat flat, Counter counter); // stany        void onFragmentSettlement(Flat flat); // rozliczenia        void onFragmentRate(Flat flat); // stawki    }}